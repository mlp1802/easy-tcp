{
  "name": "easy-tcp",
  "version": "1.0.0",
  "description": "A library for passing JSON messages trough TCP/IP sockets. Contains both client and server and example code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mikkel Petersen",
  "license": "ISC",
  "dependencies": {
    "carrier": "^0.3.0",
    "prelude-ls": "^1.1.2"
  }
}
